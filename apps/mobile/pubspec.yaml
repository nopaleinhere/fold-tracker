name: mobile
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.1.0
  get_it: ^9.2.1
  flutter_riverpod: ^3.3.1
  hive_ce: ^2.19.3
  hive_ce_flutter: ^2.3.4

  # Local packages
  core:
    path: ../../packages/core
  core_ui:
    path: ../../packages/core_ui
  shared:
    path: ../../packages/shared
  feature_habit:
    path: ../../packages/feature_habit

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  riverpod_lint: ^3.1.3

flutter:
  uses-material-design: true